# Release Notes

This directory contains per-version release notes and release-specific task lists.

## Current Files

- `v1.2.6.md`: historical note for the `frame.axc.color` interface work (shipped / superseded)

## Current Package Version

Live version is tracked in repo-root `pyproject.toml` (currently **1.4.2**).
Add a new file here when starting the next release-specific task list.

## Rule

Keep one file per version and update this README when a new version note is added.
Do not leave old "next release" notes sounding active after they have shipped.
